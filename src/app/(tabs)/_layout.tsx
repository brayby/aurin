// The (tabs) group's layout is the native bottom tab bar.
// Detail screens (card/[id], reading/[spread]) live OUTSIDE this group so the
// root Stack can push them over the tab bar.
export { default } from '@/components/app-tabs';
